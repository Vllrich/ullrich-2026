---
import Layout from '../../layouts/Layout.astro';
import Maintenance from '../../components/Maintenance.astro';
import Cover from '../../components/Cover.astro';
import WhatIDo from '../../components/WhatIDo.astro';
import Perspective from '../../components/Perspective.astro';
import Services from '../../components/Services.astro';
import Work from '../../components/Work.astro';
import Clients from '../../components/Clients.astro';
import Consult from '../../components/Consult.astro';
import FAQ from '../../components/FAQ.astro';
import Contact from '../../components/Contact.astro';
import { languageAlternates } from '../../lib/i18n';
import { landingCopy } from '../../data/landing';

const maintenance = import.meta.env.PUBLIC_MAINTENANCE_MODE === 'true';
const t = landingCopy('de');
const alternates = languageAlternates('/', '/de/');

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  '@id': 'https://larsullrich.de/de/#webpage',
  url: 'https://larsullrich.de/de/',
  name: t.meta.title,
  description: t.meta.description,
  inLanguage: 'de',
  isPartOf: { '@id': 'https://larsullrich.de/#website' },
  about: { '@id': 'https://larsullrich.de/#person' },
};
---

<Layout
  title={t.meta.title}
  description={t.meta.description}
  canonical="/de/"
  lang="de"
  alternates={alternates}
>
  {maintenance ? (
    <Maintenance />
  ) : (
    <>
      <Cover lang="de" />
      <WhatIDo lang="de" />
      <Perspective lang="de" />
      <Services lang="de" />
      <Work lang="de" />
      <Clients lang="de" />
      <Consult lang="de" />
      <FAQ lang="de" />
      <Contact lang="de" />
    </>
  )}
  <script type="application/ld+json" is:inline set:html={JSON.stringify(jsonLd)} />
</Layout>
